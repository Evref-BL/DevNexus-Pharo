{
  "name": "pharo-nexus",
  "version": "0.1.0",
  "private": true,
  "description": "Developer-environment orchestrator for Vibe Kanban, PLexus, and Pharo project workspaces.",
  "license": "Apache-2.0",
  "type": "module",
  "bin": {
    "pharo-nexus": "./dist/cli.js"
  },
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "check": "npm run build && npm test"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0",
    "vitest": "^4.0.0"
  }
}

