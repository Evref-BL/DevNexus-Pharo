{
  "name": "pharo-nexus",
  "version": "0.1.0",
  "private": true,
  "description": "Pharo specialization layer for DevNexus, PLexus, and Pharo project workspaces.",
  "license": "Apache-2.0",
  "type": "module",
  "bin": {
    "pharo-nexus": "./dist/cli.js"
  },
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build:dev-nexus": "npm --prefix ../DevNexus run build",
    "prebuild": "npm run build:dev-nexus",
    "build": "tsc -p tsconfig.json",
    "pretest": "npm run build:dev-nexus",
    "test": "vitest run",
    "check": "npm run build && npm test"
  },
  "dependencies": {
    "dev-nexus": "file:../DevNexus"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0",
    "vitest": "^4.0.0"
  }
}

